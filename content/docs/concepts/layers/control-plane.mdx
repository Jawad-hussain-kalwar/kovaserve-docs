---
title: Control plane
description: Layer 2 — the licensed KovaServe control plane. The semantic and commercial center that converts substrate signals into continuity, checkpoints, routing, policy, budgets, observability, tenancy, and managed/private portability.
---

The **control plane** is Layer 2 of the KovaServe layered model and the commercial center of the system. It sits above the open [execution substrate](/docs/concepts/layers/execution-substrate) and below application and runtime layers, and its job is to convert substrate signals into the semantics that make long-running AI execution productizable: continuity, checkpoint-aware resumability, budget-aware execution control, governance, observability, and managed/private deployment portability. It is the system of record for every entity in the canonical execution model — `Run`, `Session`, `Step`, `ModelCall`, `ContextManifest`, `ContextSegment`, `ContextArtifact`, `CompactionRecord`, `Checkpoint`, `PolicyScope`, `Budget`, `ContextBudget`, `PlacementHint`, `StateHandle`.

```
┌─ Layer 2 — KovaServe control plane ───────────────────────────────────────────┐
│                                                                               │
│   API Gateway                                                                 │
│       │  ┌── OpenAI-compatible: /v1/chat/completions, /v1/models              │
│       │  └── Native:             /v1/ks/...                                   │
│       ▼                                                                       │
│   Canonical Execution Service     ─── Run / Session / Step / ModelCall        │
│       │                                                                       │
│       ▼                                                                       │
│   Context engineering subsystem                                               │
│    ├── Global Context Manager (GCM v2)                                        │
│    ├── Context Manifest Registry                                              │
│    ├── Context Artifact Store                                                 │
│    ├── Context Admission Service                                              │
│    └── Compaction Lineage Service                                             │
│       │                                                                       │
│       ▼                                                                       │
│   Routing & state                                                             │
│    ├── KV Event System ── ingests kv_events from Layer 1                      │
│    ├── KV Index Service                                                       │
│    ├── Cache-Aware Router (manifest-aware)                                    │
│    ├── Session Registry                                                       │
│    ├── Checkpoint Manager                                                     │
│    ├── State Handle Registry                                                  │
│    └── Tiering / State Manager                                                │
│       │                                                                       │
│       ▼                                                                       │
│   Governance & accounting                                                     │
│    ├── Policy / Governance Layer                                              │
│    ├── Metering / Cost / Budget Layer                                         │
│    └── Tenant Management                                                      │
│       │                                                                       │
│       ▼                                                                       │
│   Surfaces                                                                    │
│    ├── Runtime Integration Adapters                                           │
│    ├── Dashboard / Admin Backend                                              │
│    └── SDK / CLI support APIs                                                 │
└───────────────────────────────────────────────────────────────────────────────┘
```

The control plane ships as a modular monolith — a single Python/FastAPI process composed of cleanly bounded modules that talk through `Protocol` interfaces — paired with per-node agents and per-process workers where physical separation is required. The semantic surface is identical in [managed cloud](/docs/concepts/deployment/managed-cloud) and [private deployment](/docs/concepts/deployment/private-deployment); only the operational topology differs.

## Subsystems

The control plane is composed of the following subsystems. Each subsystem has a clear ownership boundary and a stable API to its neighbors.

### API Gateway

The Gateway is the canonical entry point into the control plane. It hosts both the OpenAI-compatible inference surface and the KovaServe-native control-plane surface.

| Responsibility | Detail |
|---|---|
| Authentication | Bearer keys against the [Tenant Management](#tenant-management) hierarchy. |
| Tenancy resolution | Resolves `(tenant_id, project_id, environment_id)` from the key on every call. |
| Request normalization | Strips the [`kovaserve` extension envelope](/docs/api/index#extension-envelope) before forwarding to the substrate; surfaces it as canonical metadata. |
| Runtime metadata ingestion | Accepts the 16-field runtime metadata set used by [BYO runtime](/docs/concepts/deployment/byo-runtime) integrations. |
| Idempotency handling | Honors the [idempotency contract](/docs/api/using-the-api/idempotency) per `(tenant_id, idempotency_key)`. |
| Streaming | SSE conventions, including final-chunk cost delivery. |
| Front-door policy checks | Coarse `PolicyScope` checks before deeper subsystems are invoked. |
| Tracing | OpenTelemetry trace correlation across the entire control plane. |

The Gateway is a thin discriminator and ingress. Discrimination rule: `kovaserve` extension field present on `/v1/chat/completions` engages full canonical execution with `Run` / `Step` / `ModelCall` records; absent engages anonymous mode (still metered, still tenanted, no `Run` row).

### Canonical Execution Service

Owns authoritative records for [`Run`](/docs/concepts/run-hierarchy/runs), [`Session`](/docs/concepts/continuity/sessions), [`Step`](/docs/concepts/run-hierarchy/steps), and [`ModelCall`](/docs/concepts/run-hierarchy/model-calls). It is the semantic ledger of execution.

| Responsibility | Detail |
|---|---|
| Entity creation | Run on explicit `POST /v1/ks/runs` or implicit creation from the first inference call carrying `kovaserve.run_id`. |
| State transitions | The 12-state [Run FSM](/docs/concepts/run-hierarchy/run-state-machine), the [Step FSM](/docs/concepts/run-hierarchy/step-state-machine), and the lifecycle timestamps. |
| Lineage | `branch_from` for [branch lineage](/docs/concepts/recovery/branches); `restored_from` pointing at the source [Checkpoint](/docs/concepts/recovery/checkpoints) on resume. |
| External references | `external_run_id` and `external_runtime` for runtimes integrated via [Runtime Integration Adapters](#runtime-integration-adapters). |
| Timeline reconstruction | Embedded Steps and ModelCalls on `GET /v1/ks/runs/{id}` so clients reconstruct without N+1 calls. |
| Deferred Step admission | Step rows are created only after admission succeeds — a rejected admission inserts no Step. |

Every Step, ModelCall, Checkpoint, and timeline event in the system traces back to exactly one Run owned by this service.

### Global Context Manager (GCM v2)

GCM is the producer of [`ContextManifest`](/docs/concepts/context/context-manifests) records. It is the execution-boundary owner of context shape, identity, and accounting.

| Responsibility | Detail |
|---|---|
| Validate | Inbound context blocks against the canonical `ContextType` matrix. |
| Normalize | Segment metadata — digest, token count, byte count, checkpoint relevance, scope, retention. |
| Identify | Assign segment IDs and content digests. |
| Order | Deterministically order segments per ADR-003. |
| Identify (prefix) | Compute `stable_prefix_id` for prefix-based cache routing. |
| Identify (compatibility-aware) | Compute `stable_context_id` from the digest cascade plus renderer version, model family, tokenizer fingerprint, and Tenant/Project/context scope. |
| Account | Stable, volatile, active, and offloaded token counts. |
| Materialize | Artifact-backed segments by reference (preview text, never full payload). |
| Produce | The durable [`ContextManifest`](/docs/concepts/context/context-manifests) consumed by Admission, Router, Checkpoint Manager, and the runtime. |
| Expose | Checkpoint-relevant segment refs to the Checkpoint Manager. |
| Render | Deterministic model-dispatch output with multi-tier prefix breakpoints (tools / system / history-prefix / summary) so prefix-aligned routing and Anthropic-style prompt cache hints work from the same data structure. |

Frameworks own retrieval; runtimes own prompt logic and memory; the [Compaction Lineage Service](#compaction-lineage-service) owns the lineage contract.

### Context Manifest Registry

Durable storage for every produced `ContextManifest`, queryable by `run_id`, `step_id`, `model_call_id`, and `checkpoint_id`. Every [`ModelCall`](/docs/concepts/run-hierarchy/model-calls) carries a `context_manifest_id` foreign key so the audit trail links every model call to the exact manifest it consumed.

### Context Artifact Store

Object-storage-backed externalized context. [`ContextArtifact`](/docs/concepts/context/context-artifacts) shape: `name + version + mime_type + bytes` (Google ADK convergence). Used for raw tool outputs, file diffs, workspace snapshots, large transcripts, retrieved documents, compaction inputs, and pre/post-compaction summaries. Artifact bytes live in MinIO (managed) or customer-supplied S3-compatible storage (private). Manifests reference artifacts via `artifact_ref_id` on the segment.

### Context Admission Service

Pre-dispatch admission against [`ContextBudget`](/docs/concepts/policy/context-budgets). Decisions and their semantics:

| Decision | Meaning |
|---|---|
| `ALLOW` | Manifest fits the budget; dispatch proceeds. |
| `WARN` | Approaching limits; informational only — never blocks. |
| `OFFLOAD_REQUIRED` | Transient; the runtime offloads to artifacts and the manifest is re-admitted. |
| `COMPACT_REQUIRED` | Transient; the runtime compacts and the manifest is re-admitted. |
| `DEGRADE` | Transient; `max_tokens` reduced or optional segments dropped, then re-admitted. |
| `HARD_STOP` | Terminal; the request is rejected before GPU dispatch. |

The decision hits the wire as a [`ContextBudgetDecision`](/docs/concepts/policy/context-budget-state-machine) and as the FSM action being taken. Admission runs against the *assembled* manifest before the run-level [`Budget`](/docs/concepts/policy/budgets) precheck, so cost decisions are made on real token counts, not raw client message estimates.

### Compaction Lineage Service

[`CompactionRecord`](/docs/concepts/context/compaction-records) storage. Records source manifest, source segment IDs, output segment IDs, method, model used, token counts before and after, quality flags. The runtime may produce summaries and report records, or KovaServe may run a managed compactor — the lineage contract is the same in both cases. The contract is what makes resume reconstructible: a Checkpoint can rebuild logical context from manifest + artifacts + compaction lineage even when KV restore is unavailable.

### KV Event System

Ingests and normalizes the `kv_events` stream from [Layer 1](/docs/concepts/layers/execution-substrate). Responsibilities: durable ingestion, event enrichment and validation, replayable streams, and fan-out to KV Index, Metering, Dashboard, and audit consumers. The system is event-driven on real substrate truth. See [state events](/docs/concepts/events/state).

### KV Index Service

Maintains near-real-time authoritative lookup of reusable state. Maps stable identities to nodes and tiers; tracks freshness, completeness, compatibility, and residency; answers lookup queries for routing and resume planning; resolves activation [`StateHandle`](/docs/concepts/recovery/state-handles)s. See [KV Index](/docs/concepts/routing/kv-index).

### Cache-Aware Router (manifest-aware)

Chooses execution placement using continuity, context reuse value, and economics — not blind load-balancing or raw prefix hash. Score inputs:

| Input | Source |
|---|---|
| Cache hit potential | KV Index lookup on `stable_prefix_id`. |
| Manifest reuse estimate | `stable_context_id` match across breakpoint tiers. |
| Expected prefill savings tokens | Manifest token accounting. |
| Rehydrate / restore cost | KV state restore latency + artifact reload bytes. |
| Queue cost | Substrate node telemetry. |
| Isolation penalty | Tenancy and PolicyScope scope filter. |
| Model availability | Substrate model lifecycle signals. |
| Node health | Substrate telemetry. |
| Placement hints | Runtime-supplied `PlacementHint`. |
| Resume priority | Whether the request is a resume from `Checkpoint`. |
| Policy constraints | `policy_scope.context_share_scope`, `data_boundary`. |
| Run-level Budget pressure | Metering layer. |
| `ContextBudget` admission state | Degraded runs prefer cache reuse more aggressively. |
| Session affinity | Session Registry, when bound. |

Outputs: selected route, fallback route(s), explanation rationale (`route.rationale`), warm/partial/cold classification, `resume_feasible` flag. See [manifest-aware routing](/docs/concepts/routing/manifest-aware-routing).

### Session Registry

Persists continuity over time. Manages [`Session`](/docs/concepts/continuity/sessions)s and their active state handles, tracks the Session's `current_manifest_id` and bounded manifest history, tracks `CompactionRecord`s associated with the Session, tracks `ContextArtifact` references retained at Session scope, treats summaries as first-class typed segments, binds Sessions to Runs and Checkpoints, and supports Session survival across node restarts.

### Checkpoint Manager

Makes resumability first-class. **Logical context is the primary capability; KV cache export/import is an optimization layered on top.** A checkpoint must remain useful even when KV restore is unavailable. Responsibilities:

| Responsibility | Detail |
|---|---|
| Create checkpoint manifests | Bound to a frozen `ContextManifest` snapshot. |
| Bind references | Run / Step / Session / state / artifact / compaction refs. |
| Validate compatibility | Model family, tokenizer fingerprint, renderer version, scope. |
| Plan restore | Logical context manifest first, KV state if available. |
| Track outcomes | Restore attempts, latencies, failure reasons. |
| Expose StateHandles | Typed `kv_prefix | context_manifest | checkpoint | artifact_bundle | runtime_state` references. |

Resume order is logical-first: load manifest → validate manifest and artifacts → validate model/tokenizer compatibility → plan KV restore if available → reconstruct active context from manifest+artifacts if KV restore is unavailable → route based on feasibility → transition through `RECOVERING → RESUMING → RUNNING`. See [resume flow](/docs/concepts/recovery/resume-flow).

### State Handle Registry

Resolves typed [`StateHandle`](/docs/concepts/recovery/state-handles)s. Discriminator values: `kv_prefix`, `context_manifest`, `checkpoint`, `artifact_bundle`, `runtime_state`. Generalizes the original "activation handle" concept so cross-session reuse extends beyond raw KV prefixes to manifests, artifacts, and runtime state.

### Tiering / State Manager

Preserves continuity under pressure. Observes tier occupancy and pressure, plans demotion / promotion / archive / prefetch, instructs Layer 1 actions, preserves state identity across movement, exposes transfer and restore activity to observability and metering. The Tiering Manager is a **planner/controller** in Layer 2; raw movement execution belongs to the Layer 1 Transfer/Restore Worker adjacency.

### Policy / Governance Layer

| Responsibility | Detail |
|---|---|
| Model ACLs | Which models a [`PolicyScope`](/docs/concepts/policy/policy-scopes) may dispatch against. |
| Rate limits | Per Tenant / Project / Environment. Most-specific applicable limit wins. |
| Share scope | `policy_scope.context_share_scope` — controls cache and manifest reuse boundaries (`tenant`, `project`, `environment`, `none`). |
| Tool policy | `tool_policy_ref` on the resolved PolicyScope. |
| Data boundary | Artifact and context residency constraints. |
| Environment / deployment restrictions | Region pinning, sovereign overlays. |
| Decisions | Explainable allow / deny / degrade — fast, deterministic, auditable. |

### Metering / Cost / Budget Layer

The economic system of record — active control objects with real-time enforcement on every request.

| Responsibility | Detail |
|---|---|
| Ingest raw usage | With OpenAI-convergent `cached_tokens`. |
| Aggregate | By Tenant / Project / model / day. |
| Attach | To [`Budget`](/docs/concepts/policy/budgets)s and [`ContextBudget`](/docs/concepts/policy/context-budgets)s. |
| Forecast | Exhaustion windows. |
| Emit | Warnings and hard stops as runtime-visible decisions. |
| Track savings | Cache reuse value, manifest reuse value, compaction savings. |
| Track context economics | Active tokens, stable tokens, volatile tokens, offloaded tokens, artifact storage bytes. |
| Roll up | `cost_per_completed_run` per Run, per Project, per Tenant. |
| Snapshot pricing | `input_price_snapshot`, `output_price_snapshot`, `discount_snapshot` per usage row, so audit is row-self-contained. |
| Preserve identity | `gross = net + savings` exactly across rows and scopes. |

`Budget`s govern run-level cost / tokens / steps / time / tools. `ContextBudget`s govern admission control on context size. They coexist; they evaluate at different points.

### Tenant Management

Owns the [tenancy](/docs/concepts/tenancy) hierarchy: [`Tenant`](/docs/concepts/tenancy/tenants) → [`Project`](/docs/concepts/tenancy/projects) → [`Environment`](/docs/concepts/tenancy/environments) → `Deployment`. Bearer [`Keys`](/docs/concepts/tenancy/keys) authenticate against the resolved triple. Resolves scope on every request; manages role bindings; partitions partner-safe deployments; supports both managed multi-tenancy and private single-tenant topologies.

### Runtime Integration Adapters

Mandatory for [BYO runtime mode](/docs/concepts/deployment/byo-runtime). Ingest metadata from external runtimes, map external execution IDs into canonical entities, preserve checkpoint / budget / placement hints, and normalize semantics without taking over orchestration. Adapters exist for LangGraph, Temporal, and a generic runtime envelope; the wire shape is uniform across them.

### Dashboard / Admin Backend

Surfaces every control-plane truth that operators and developers need:

| View | Purpose |
|---|---|
| Run explorer | Per-Run timeline, Step list, ModelCalls, Checkpoints, branches. |
| Session explorer | Manifest history, current_manifest_id, retained artifacts. |
| Checkpoint browser | Tree view, restore feasibility, attached StateHandles. |
| State / routing view | Where reusable state lives, routing decisions, fallbacks. |
| Usage and budget dashboards | Per-Run, Project, Tenant roll-up. |
| Context manifest timeline | Per Run — segments, sizes, tier classification. |
| Compaction lineage visualization | Source-to-output relationships. |
| Artifact offload visibility | Bytes, types, retention. |
| Context budget burn | Per Run — admission decisions over time. |
| Policy decision inspection | Per call — what was allowed, denied, degraded, why. |
| Fleet / node health | From substrate telemetry. |
| Tenant / Project admin | Hierarchy management, key issuance. |

### SDK / CLI support APIs

The control-plane endpoints that back the [Python SDK](/docs/sdk) and the [`kova` CLI](/docs/cli). Wire shapes for Run, Session, Checkpoint, Budget, Context, Artifact, Compaction, StateHandle, and Events.

## Public API surface

The Gateway exposes three families of endpoints. All wire shapes are stable across managed cloud and private deployment.

| Family | Examples | Voice |
|---|---|---|
| **OpenAI-compatible** | `POST /v1/chat/completions`, `GET /v1/models` | Drop-in for BYO runtime adoption. Carries the optional `kovaserve` extension envelope. |
| **Native control plane** | `POST /v1/ks/runs`, `GET /v1/ks/runs/{id}`, `POST /v1/ks/sessions`, `POST /v1/ks/budgets`, `POST /v1/ks/policies` | First-class control over canonical entities. |
| **Context engineering** | `POST /v1/ks/context/manifests`, `POST /v1/ks/context/admit`, `POST /v1/ks/context/compact`, `GET /v1/ks/handles/{id}` | Manifest, artifact, admission, compaction, state handle. |
| **Runtime APIs** | `POST /v1/ks/runtime/runs`, `POST /v1/ks/runtime/runs/{id}/pause`, `/resume`, `/branch`, `/timeline`, `/artifacts` | The Layer 3 runtime surface, hosted on the same gateway. |
| **Events (pulled)** | `GET /v1/ks/events/...` | The event streams as a polled API. |
| **Health** | `GET /health`, `GET /metrics` | Liveness, readiness, Prometheus metrics. |

The native APIs share the [error envelope](/docs/api/using-the-api/errors), [pagination](/docs/api/using-the-api/pagination), [idempotency](/docs/api/using-the-api/idempotency), [cost headers](/docs/api/using-the-api/cost-headers), and [SSE streaming](/docs/api/using-the-api/streaming) conventions.

## Event taxonomy

Layer 2 emits multiple event streams under the `ks.>` subject namespace, plus the `kv_events` substrate stream it ingests from Layer 1. Subjects share the [event envelope](/docs/concepts/events/event-envelope) — `event_id`, `event_type`, timestamp, tenancy fields, canonical-entity refs, `trace_id`, payload.

| Stream | Examples | Concept |
|---|---|---|
| Execution | `ks.run.created`, `ks.step.completed`, `ks.model_call.completed` | [Execution events](/docs/concepts/events/execution) |
| State (`kv_events`) | `kv.state.stored`, `kv.state.restored` | [State events](/docs/concepts/events/state) |
| Routing | `ks.route.planned`, `ks.route.fallback_used` | [Routing events](/docs/concepts/events/routing) |
| Policy | `ks.policy.evaluated`, `ks.policy.denied` | [Policy events](/docs/concepts/events/policy) |
| Budget | `ks.budget.warning`, `ks.budget.hard_stop` | [Budget events](/docs/concepts/events/budget) |
| Checkpoint | `ks.checkpoint.created`, `ks.checkpoint.restore_completed` | [Checkpoint events](/docs/concepts/events/checkpoint) |
| Runtime | `ks.tool.invocation.started`, `ks.process.waiting`, `ks.file.write.applied`, `ks.branch.created` | [Runtime events](/docs/concepts/events/runtime) |
| Context engineering | `ks.context.manifest.created`, `ks.context.budget.compact_required` | [Context engineering events](/docs/concepts/events/context-engineering) |

## The hot path

The control plane orders the standard model call so context-dependent decisions run after context is assembled — never against rough message estimates.

```
1.  Authn / scope resolution           ── Gateway: resolve Tenant / Project / PolicyScope
2.  Run / Session resolution            ── Canonical Execution Service (no Step yet)
3.  Context inputs gathering            ── Session history + ContextArtifact previews
4.  Manifest assembly (GCM v2)          ── ContextSegments, stable_prefix_id, stable_context_id, token counts
5.  Context admission                   ── ContextBudget decision; transformations applied; manifest updated
6.  Run-budget precheck                 ── Budget evaluates against admitted manifest
7.  Step creation                       ── Step row created and bound to manifest
8.  Routing                             ── Cache-Aware Router with KV Index + scope filters + budget pressure
9.  Substrate execution                 ── Layer 1 executes; emits real lifecycle / state events
10. Recording                           ── ModelCall stored with context_manifest_id FK; metering, ContextBudget
                                            consumption, ExecutionEvents, Session updates, dashboards update
```

## Deployment

The control plane is one Helm chart. On [managed cloud](/docs/concepts/deployment/managed-cloud) it runs in KovaServe's hosted Kubernetes cluster against hosted Postgres / Redis / NATS / MinIO. On [private deployment](/docs/concepts/deployment/private-deployment) it runs in the customer's k3s or conformant Kubernetes cluster against customer-supplied Postgres / Redis / NATS / S3-compatible storage. The reference 3-VM split is one control node plus two GPU nodes; the chart parameterizes the layout.

Subsystem boundaries map cleanly to deployment: every Layer 2 subsystem is a module in the FastAPI monolith, every Layer 1 component is a per-node agent or worker, and every cross-process call goes through gRPC, NATS, or ZMQ — never an in-process event bus.

## See also

- [Layered model](/docs/concepts/layers) — the three-layer architecture this page details Layer 2 of
- [Execution substrate](/docs/concepts/layers/execution-substrate) — Layer 1, the supplier of substrate signals
- [Runtime wedge](/docs/concepts/layers/runtime-wedge) — Layer 3, the focused runtime built on top
- [Concepts overview](/docs/concepts) — the canonical entity map
- [Runs](/docs/concepts/run-hierarchy/runs) · [Sessions](/docs/concepts/continuity/sessions) · [Steps](/docs/concepts/run-hierarchy/steps) · [ModelCalls](/docs/concepts/run-hierarchy/model-calls) — the canonical execution entities the control plane persists
- [ContextManifests](/docs/concepts/context/context-manifests) · [Checkpoints](/docs/concepts/recovery/checkpoints) · [Budgets](/docs/concepts/policy/budgets) · [PolicyScopes](/docs/concepts/policy/policy-scopes) — the load-bearing control objects
- [Managed cloud](/docs/concepts/deployment/managed-cloud) · [Private deployment](/docs/concepts/deployment/private-deployment) — the deployment topologies
- [Events overview](/docs/concepts/events) — the event streams the control plane emits
